Link para download do git: https://git-scm.com/downloads

O Git Bash é um terminal para otimizar o uso do Git.

Git Bash não é necessário porque o sistema operacional usa o terminal Bash como padrão. Para usar o Git no macOS, você pode simplesmente abrir o Terminal e usar os comandos do Git normalmente.
